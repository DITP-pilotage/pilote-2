meta {
  name: sample KO
  type: http
  seq: 2
}

get {
  url: {{sample_url}}{{todo_id}}
  body: none
  auth: none
}

vars:pre-request {
  sample_url: https://jsonplaceholder.typicode.com/todos/
  todo_id: not_a_number
}

tests {
  test("should return 404 because no resource found", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(404);
  });
}

docs {
  Une requete d'exemple avec un test.
}
