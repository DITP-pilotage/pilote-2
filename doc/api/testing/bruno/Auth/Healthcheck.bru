meta {
  name: Healthcheck
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/open-api/healthcheck
  body: none
  auth: bearer
}

auth:bearer {
  token: {{api_token}}
}

body:json {
  {
    "sfs":22
  }
}

tests {
  test("should return 200", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
  });
}

docs {
  Endpoint de Healthcheck pour tester si l'API est en fonctionnement.
}
