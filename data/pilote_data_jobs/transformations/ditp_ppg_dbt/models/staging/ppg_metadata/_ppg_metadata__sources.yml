version: 2

sources:
  - name: ppg_metadata
    schema: "{{ env_var('PGSCHEMA_RAW_DATA') }}"
    tables:
      - name: chantier_meteo
      - name: indicateur_type
      - name: metadata_axe
      - name: metadata_indicateur
      - name: metadata_perimetre
      - name: metadata_chantier
      - name: metadata_porteur
      - name: metadata_ppg
      - name: metadata_zone
