version: 2

models:
  - name: df2_indicateur
    description: "Table des indicateur construite par la datafactory dbt."
    config:
      materialized: table
      schema: public
      tags: ["df2"]
      enabled: true