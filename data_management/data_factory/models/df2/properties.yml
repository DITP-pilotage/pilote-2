version: 2

models:
  - name: df2_indicateur
    description: "Table des indicateurs construite par la datafactory dbt."
    config:
      materialized: table
      docs:
        node_color: "#cd7f32"
      schema: public
      tags: ["df2"]
      enabled: true
  - name: df2_chantier
    description: "Table des chantiers construite par la datafactory dbt."
    config:
      materialized: table
      docs:
        node_color: "#cd7f32"
      schema: public
      tags: ["df2"]
      enabled: true