describe('ImportDonneeIndicateurAPIHandler', () => {
  describe('quand on import un fichier via une donnee en JSON', () => {
    it('quand le fichier est invalide a cause de validata, doit remonter les erreurs validata', () => {});
    it('quand le fichier est invalide a cause de règle DITP <<INSERT REGLE>>, doit remonter les erreurs', () => {});
    it('quand le fichier est valide, doit importer les données', () => {});
  });
  describe('quand on import un fichier via une donnee en CSV', () => {
    it('quand le fichier est invalide a cause de validata, doit remonter les erreurs validata', () => {});
    it('quand le fichier est invalide a cause de règle DITP <<INSERT REGLE>>, doit remonter les erreurs', () => {});
    it('quand le fichier est valide, doit importer les données', () => {});
  });
});
