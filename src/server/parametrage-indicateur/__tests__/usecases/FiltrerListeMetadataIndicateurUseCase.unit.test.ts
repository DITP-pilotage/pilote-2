describe('FiltrerListeMetadataIndicateurUseCase', () => {
  it("doit renvoyer la liste des indicateurs de tous les chantiers quand aucun filtre n'est appliqué", () => {});
  it('doit renvoyer la liste des indicateurs ce tous les chantiers où le territoire est celui appliqué dans les filtres', () => {});
  it('doit renvoyer la liste des indicateurs de tous les chantiers où le ministère est celui appliqué dans les filtres', () => {});
  it('doit renvoyer la liste des indicateurs du chantier sélectionné dans les filtres', () => {});
});
