import styled from '@emotion/styled';

const DonneesPersonnellesCookiesStyled = styled.div`
    main { 
        background: var(--background-alt-blue-france);
    };
    
    .container {
        max-width: 920px;
    }

    h1, h2 {
        color: var(--text-title-blue-france);
    };
`;

export default DonneesPersonnellesCookiesStyled;
