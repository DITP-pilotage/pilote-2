import styled from '@emotion/styled';

const PictoTerritorialiséStyled = styled.span`
  color: #7B7B7B;

  &::before {
    width: 1.25rem;
    height:1.25rem;
    mask-size: contain;
  }
`;

export default PictoTerritorialiséStyled;
