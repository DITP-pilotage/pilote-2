import styled from '@emotion/styled';

const TableauStyled = styled.section`
  overflow-x: auto;

  table.tableau {
    display: table;
  }

  nav {
    button {
      border-radius: 4px;
    }
  }
`;

export default TableauStyled;
