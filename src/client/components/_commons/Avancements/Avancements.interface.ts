export default interface AvancementsProps {
  avancements: {
    moyenne: number | null,
    moyenneAnnuelle: number | null,
    médiane: number | null,
    minimum: number | null,
    maximum: number | null
  }
}
