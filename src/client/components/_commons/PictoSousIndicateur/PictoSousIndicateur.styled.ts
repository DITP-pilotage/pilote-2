import styled from '@emotion/styled';

const PictoSousIndicateurStyled = styled.span`
  color: var(--background-action-high-blue-france);

  &::before {
    width: 1.25rem;
    height: 1.25rem;
    mask-size: contain;
  }
`;

export default PictoSousIndicateurStyled;
