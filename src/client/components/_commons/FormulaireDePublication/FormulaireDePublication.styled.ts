import styled from '@emotion/styled';

const FormulaireDePublicationStyled = styled.form`
  .actions .fr-btn {
    border-radius: 4px;
  }
  
  textarea {
    max-height: 85vh;
    resize: vertical;
  }
`;

export default FormulaireDePublicationStyled;
