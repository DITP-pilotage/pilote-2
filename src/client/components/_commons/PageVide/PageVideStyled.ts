import styled from '@emotion/styled';

const PageVideStyled = styled.div`
  .fr-badge {
    background: var(--background-contrast-purple-glycine);
    color: var(--text-label-purple-glycine);
    max-height: 2rem;
  }
`;

export default PageVideStyled;
