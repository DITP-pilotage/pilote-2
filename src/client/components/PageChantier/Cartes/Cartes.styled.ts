import styled from '@emotion/styled';

const CartesStyled = styled.div`
  flex-wrap: wrap;
  row-gap: 1.5rem;
  column-gap: 1.5rem;
  
  & > * {
    flex-grow: 1;
  }
`;

export default CartesStyled;
