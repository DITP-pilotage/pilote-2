import styled from '@emotion/styled';

const PageModifierUtilisateurStyled = styled.div`
background: var(--background-alt-blue-france);

  .bouton-retour {
    &::before {
      margin-right: 0.625rem;

      --icon-size: 0.875rem;
    }

    color: var(--text-action-high-blue-france);
    background: none;
  }

`;

export default PageModifierUtilisateurStyled;
