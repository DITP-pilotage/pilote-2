import styled from '@emotion/styled';

const PageAccueilStyled = styled.div`
  background: var(--background-alt-blue-france);

  .contenu-principal {
    width: 100%;
  }
`;

export default PageAccueilStyled;
