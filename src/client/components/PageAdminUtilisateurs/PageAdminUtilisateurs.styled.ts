import styled from '@emotion/styled';

const PageAdminUtilisateursStyled = styled.div`

  .titre-tableau{
    color: var(--text-title-blue-france);
  }
`;

export default PageAdminUtilisateursStyled;
